import 'package:flutter/material.dart';

class AppColors {
  // Palet Utama: Hijau Pertanian & Daun Pisang Sehat
  static const Color primary = Color(0xFF16A34A);         // Hijau Daun Segar (Emerald 600)
  static const Color primaryDark = Color(0xFF15803D);     // Hijau Tua (Emerald 700)
  static const Color primaryForest = Color(0xFF14532D);   // Hijau Hutan (Emerald 900)
  static const Color primaryLight = Color(0xFFDCFCE7);    // Hijau Sangat Lembut (Emerald 100)
  static const Color primaryMuted = Color(0xFFF0FDF4);    // Hijau Background Lembut (Emerald 50)

  // Palet Aksen: Kuning Pisang Matang
  static const Color bananaYellow = Color(0xFFEAB308);    // Kuning Pisang (Yellow 500)
  static const Color bananaGold = Color(0xFFF59E0B);      // Emas Pisang Matang (Amber 500)
  static const Color bananaLight = Color(0xFFFEF08A);     // Kuning Muda (Yellow 200)
  static const Color bananaCream = Color(0xFFFEFCE8);     // Krim Pisang (Yellow 50)

  // Latar Belakang & Permukaan Kartu
  static const Color background = Color(0xFFF5F8F5);      // Abu-abu hijau alam sangat lembut
  static const Color cardSurface = Colors.white;          // Kartu putih bersih
  static const Color cardBorder = Color(0xFFE2E8F0);       // Garis batas kartu halus
  static const Color inputBackground = Color(0xFFF9FAF9); // Kolom isian bersih

  // Teks & Typografi
  static const Color textPrimary = Color(0xFF1E293B);     // Teks utama hitam-slate kontras
  static const Color textSecondary = Color(0xFF64748B);   // Teks pendukung abu-abu
  static const Color textMuted = Color(0xFF94A3B8);       // Teks placeholder / hint

  // Status & Indikator
  static const Color success = Color(0xFF22C55E);         // Sukses
  static const Color error = Color(0xFFEF4444);           // Error / Gagal
  static const Color warning = Color(0xFFF97316);         // Peringatan
}

